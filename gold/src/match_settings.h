#pragma once

enum MatchSetting {
    MATCH_SETTING_MAP_SIZE,
    MATCH_SETTING_TEAMS,
    MATCH_SETTING_COUNT
};

enum MapSize {
    MAP_SIZE_SMALL,
    MAP_SIZE_MEDIUM,
    MAP_SIZE_LARGE,
    MAP_SIZE_COUNT
};

enum MatchSettingTeams {
    TEAMS_ENABLED,
    TEAMS_DISABLED,
    TEAMS_COUNT
};